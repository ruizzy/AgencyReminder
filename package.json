{
  "name": "agency-reminder",
  "version": "1.0.0",
  "description": "日常工作待办提醒工具",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "mkdir -p dist && copy public\\index.html dist\\index.html",
    "cap:sync": "npx cap sync",
    "cap:open": "npx cap open android",
    "cap:build": "cd android && .\\gradlew assembleDebug"
  },
  "dependencies": {
    "axios": "^1.6.7",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "node-cron": "^3.0.3"
  },
  "devDependencies": {
    "@capacitor/android": "^5.7.8",
    "@capacitor/cli": "^5.7.8",
    "@capacitor/core": "^5.7.8"
  }
}
